const { expect } = require('chai');

describe('A test', () => {
  it('Tests something', () => {
    expect('a').to.equal('a');
  });
  it('Tests something else', () => {
    expect('a').to.equal('a');
  });
});
